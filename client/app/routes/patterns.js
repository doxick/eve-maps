import { createPattern } from 'redux-router'

export const Homepage = createPattern('/')
export const MultiMap = createPattern('/multi-map')

export const AuthenticateCallback = createPattern('/authenticate/callback')
